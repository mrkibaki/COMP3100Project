import java.io.*;
import java.net.*;

public class dispatcher {
    public void dispatch(DataOutputStream dout, BufferedReader din, String command, String str) {
        // dout.write((command).getBytes());
        // dout.flush();
        // str = din.readLine();
        // System.out.println("RCVD: " + str);
    }
}
